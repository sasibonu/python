if __name__ == '__main__':
    l=[]
    s=[]
    n=int(input())
    for i in range(n):
        name = input()
        score = float(input())
        s=[name,score]
        l.append(s)   
    def Sort(l):
 
     return(sorted(l, key = lambda x: x[1]))    
 
    l=Sort(l)
    l.remove(l[0])
    n=len(l)
    k=l[0]
    t=k[1]
    a=list(filter(lambda i: i[1] == t, l))
    b=(sorted(a, key = lambda x: x[0]))
    for i in range(len(b)):
        print(b[i][0])
